# Morning Review - 2026-05-05

## Phase A+ Readiness Summary

Overnight operating mode was resumed at 2026-05-04 22:35 CST with the Phase A+
directive constrained to two tasks: XLM-RoBERTa multilingual baseline and A8
cross-document dataloader preparation. Data augmentation remained closed.

## XLM-RoBERTa Blind Spot

Command family:

```bash
PYTORCH_JIT=0 uv run python train_span.py --dataset accord --model-name xlm-roberta-base \
  --bio-weight 0.1 --neg-sample-ratio 3.0 --max-steps 3500 --seed <seed>
```

Four-seed CODE-ACCORD dev Triple F1:

| Seed | Dev Triple F1 | Test Triple F1 |
| --- | ---: | ---: |
| 42 | 0.2871 | 0.0856 |
| 43 | 0.3425 | 0.0970 |
| 44 | 0.3598 | 0.1136 |
| 45 | 0.3423 | 0.0645 |

Mean dev Triple F1 was 0.3329 +/- 0.0316, comparable to the BERT-base
8-seed baseline of 0.329 +/- 0.028. This validates the multilingual tokenizer
and backbone path for Phase B, but it is not a model-quality improvement.

## A8 Sliding Window Dataloader

`data/code_accord.py` now supports document-aware sliding windows via
`--doc-window-size` and `--doc-window-stride`. With `--doc-window-size 2`, each
sample is the current sentence plus the next same-document sentence. Entity and
relation spans are shifted by the word offset of their source sentence inside
the joined context. Relations remain sentence-local because CODE-ACCORD does
not annotate cross-sentence relation pairs.

Exact dry-run command:

```bash
PYTORCH_JIT=0 uv run python train_span.py --dataset accord \
  --model-name microsoft/deberta-base --bio-weight 0.1 \
  --neg-sample-ratio 3.0 --doc-window-size 2 --doc-window-stride 1 \
  --max-length 256 --max-steps 200 --seed 42
```

Dry-run result:

- CODE-ACCORD windows compiled: train=720, dev=126, test=173.
- Relation labels survived offsetting: train rels=2241, dev rels=391.
- Dev and test metric calculation completed without crashing.
- Final dev NER=0.4489, Triple=0.0000.
- Final test NER=0.4679, Triple=0.0000.

Triple=0 is expected for a 200-step compile smoke and should not be interpreted
as a quality comparison.

## Literature ingested overnight

The initial Phase A+ task block did not trigger a literature search because it
was a constrained infrastructure/baseline directive. After the soft milestone
handoff, two non-improving A8 diagnostics triggered the Knowledge Pipeline.

New lookup saved:

- `sources/papers_20260504_cross_doc_re_provenance_graphrag.md`

New wiki/raw note:

- `wiki/raw/KXDocRE_cross_document_RE_2024.md` — Jain et al.,
  "Knowledge-Driven Cross-Document Relation Extraction," Findings of ACL 2024,
  DOI 10.18653/v1/2024.findings-acl.227.

Key insight: longer context alone is not enough. KXDocRE suggests ranking
evidence paths between entity pairs before relation prediction. For CODE-ACCORD,
the direct Wikidata-path mechanism is not transferable, but the idea maps to
document-local paths: same `doc_id`, repeated entity mentions, source-sentence
provenance, schema-compatible relation labels, and one/two-hop extracted KG
edges. Next best diagnostic is top-k evidence-path coverage before any new
training or augmentation.

## Decision

Phase A+ is ready to hand to Phase B infrastructure work:

- XLM-R is pipeline-safe for Chinese/English mixed engineering specs.
- Two-sentence same-document windows are implemented and smoke-tested.
- Data augmentation remains closed after the 8-seed combined augmentation
  failure.
- Best validated quality result remains DeBERTa-large plain baseline
  0.3797 +/- 0.0346; schema-aware augmentation is only a weak stability signal,
  not a locked breakthrough.

## Phase B ROI Brainstorm

Candidate directions:

1. Build the Chinese/English mixed-spec ingestion path on top of the validated
   XLM-R tokenizer/backbone path, using existing CODE-ACCORD labels first.
2. Promote the two-sentence sliding window into the Phase B extraction pipeline
   and inspect pronoun/entity carryover errors before any quality tuning.
3. Add document-level provenance checks to KG construction so cross-sentence
   context can be traced in Graph RAG failures.

Best ROI next step: promote the A8 two-sentence window into Phase B extraction
and provenance diagnostics. It directly targets the isolated-reference failure
mode, reuses the smoke-tested dataloader, and avoids the now-closed augmentation
path.

## Post-Handoff Phase B Diagnostics

After the soft milestone handoff, A8 was promoted into extraction:

- `inference_kg.py` now accepts `--doc-window-size`, `--doc-window-stride`,
  `--max-length`, and per-seed split control.
- CODE-ACCORD batches now preserve `doc_id` and `example_id` metadata for KG
  provenance.
- DeBERTa-large s42 doc-window inference on test produced 173 windows, 133
  entities, and 53 triples; best confidence-sweep Triple F1 remained 0.090 at
  conf>=0.7, matching prior sentence-level smoke behavior.
- Conf>=0.7 doc-window KG had 58 nodes / 39 edges. 25Q RAG with edge-source
  context gave text=16%, kg_1hop=4%, kg_2hop=4%, hybrid=20%,
  hybrid_edge_source=20%.
- New `diagnose_evidence_paths.py` measured evidence coverage before LLM
  answering: head node recall=34.8%, tail reachable in top-10 paths=13.0%,
  relation present in top-10=17.4%, source tail present=12.2%.

Conclusion: A8 infrastructure is functional, but longer context/provenance does
not fix ACCORD KG coverage. The next ROI is coverage-oriented extraction
diagnostics, not prompt changes or data augmentation.
