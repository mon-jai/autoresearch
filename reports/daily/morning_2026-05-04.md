# Morning Review - 2026-05-04

## Phase A+ Readiness

Overnight mode began Phase A+ with two infrastructure goals: verify the
multilingual path with XLM-RoBERTa and prepare A8 document-level context windows
for CODE-ACCORD.

## XLM-RoBERTa Blind Spot

Command family:
`train_span.py --dataset accord --model-name xlm-roberta-base --bio-weight 0.1 --neg-sample-ratio 3.0 --max-steps 3500`

| Seed | Best step | Dev NER | Dev Triple | Test Triple |
|------|-----------|---------|------------|-------------|
| 42 | 2100 | 0.5737 | 0.2871 | 0.0856 |
| 43 | 3200 | 0.6278 | 0.3425 | 0.0970 |
| 44 | 2900 | 0.6345 | 0.3598 | 0.1136 |
| 45 | 2800 | 0.6198 | 0.3423 | 0.0645 |
| **Mean** | - | - | **0.3329 +/- 0.0316** | - |

Interpretation: XLM-R is comparable to the BERT-base 8-seed CODE-ACCORD
baseline (`0.329 +/- 0.028`). The mixed Chinese/English Phase B transition is
safe from a tokenizer/backbone plumbing standpoint.

Residual risk: test Triple remains low for XLM-R, as it does for existing
encoders. This confirms the bottleneck is still extraction coverage/generalizing
relations, not tokenizer compatibility.

## A8 Document Window Prep

Implemented opt-in consecutive-sentence context windows for CODE-ACCORD:

- `--doc-window-size N` joins up to N consecutive sentences from the same source
  document.
- `--doc-window-stride` controls sliding-window stride.
- Entity and relation word spans are offset into the joined context.
- CSV metadata document IDs are parsed from `metadata['ID']`.
- Original sentence order is preserved before windowing.

Validation:

- `xlm-roberta-base` and `bert-base-uncased` loader smoke tests passed with
  `doc_window_size=3`, `max_length=256`.
- A 3-step BERT training dry-run with `--doc-window-size 3` completed
  dev/test metric calculation without errors.

## Literature ingested overnight

No new papers were ingested during this Phase A+ run. Reason: the session was a
planned blind-spot baseline plus dataloader infrastructure task; the Knowledge
Pipeline trigger threshold of 2 stuck experiments was not reached.

Relevant previously ingested material for the next step remains:

- `wiki/raw/KG_RE_Coverage_Augmentation_20260501.md`: KG extraction coverage and
  low-resource augmentation notes.
- `sources/papers_20260502_graphrag_kg_retrieval_low_resource_ie.md`: Graph RAG
  and low-resource IE retrieval survey notes.

## Recommended Next ROI

Run a 4-seed document-window comparison on CODE-ACCORD with `max_length=256`:

- BERT-base sentence-level baseline control if needed for exact comparability.
- BERT-base or XLM-R with `--doc-window-size 3 --doc-window-stride 1`.

Success criterion: improve dev Triple mean without increasing variance enough to
erase the gain. If document windows underperform, the next best ROI remains
data-volume/schema-aware augmentation rather than more loss/head changes.
